swagger:
  protocol: http
  base:
    host: localhost
elastic:
  apm:
    enabled: 'true'
    server-url: 'http://localhost/8200'
    service-name: 'jwt-decode'
    secret-token: 'bXlfYXBtX3NlY3JldF90b2tlbiAtbgo='
    environment: 'local'
    log-sending: 'true'
    capture-body: 'all'
